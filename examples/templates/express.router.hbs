const router = require('express').Router

const {{camelCase router}} = new Router()

{{camelCase router}}.get('/', (req, res) => {
  res.render('Hello ' + '{{pascalCase router}}')
})

module.exports = {
  {{pascalCase router}}
}